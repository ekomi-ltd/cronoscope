{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch program",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/src/main.go"
        }
    ]
}